import { styled } from '@mui/material/styles';

export const MainContainer = styled('div')({
  width: '100%',
});

export const smMarginStyle = (theme) => ({
  margin: theme.spacing(1),
});

export const ActionDiv = styled('div')({
  textAlign: 'center',
});